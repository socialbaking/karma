import { ReportDateData } from "./types";

export const REPORT_DATE_KEYS: (keyof ReportDateData)[] = [
    "createdAt",
    "receivedAt",
    "updatedAt",
    "orderedAt",
    "shippedAt",
    "reportedAt"
]